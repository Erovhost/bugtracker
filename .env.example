# Образец настроек. Скопируйте в .env и подставьте свои значения.
# Файл .env в репозиторий не попадает (см. .gitignore).

# Секретный ключ Flask: подпись сессий и защита форм (CSRF).
# Сгенерировать: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-with-random-string

# Подключение к PostgreSQL:
# postgresql+psycopg://ПОЛЬЗОВАТЕЛЬ:ПАРОЛЬ@СЕРВЕР:ПОРТ/ИМЯ_БАЗЫ
DATABASE_URL=postgresql+psycopg://bugtracker:your-password@localhost:5432/bugtracker

# Роли с ограниченными правами (создаются скриптом sql/roles.sql).
# Нужны для проверки прав; DATABASE_URL выше остаётся ролью владельца —
# под ней выполняются миграции.
APP_DATABASE_URL=postgresql+psycopg://bugtracker_app:your-app-password@localhost:5432/bugtracker
READONLY_DATABASE_URL=postgresql+psycopg://bugtracker_readonly:your-readonly-password@localhost:5432/bugtracker

# Отдельная база для тестов pytest (тесты стирают в ней данные!).
# Имя базы обязательно должно заканчиваться на _test.
TEST_DATABASE_URL=postgresql+psycopg://bugtracker:your-password@localhost:5432/bugtracker_test

# Журнал в консоль вместо файла logs/bugtracker.log — для хостинга (Render),
# где файлы стираются при перезапуске. Локально не нужен.
# LOG_TO_STDOUT=1
