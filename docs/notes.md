# Заметки по этапам (для отчётов по практике)

## Этап 0. Окружение и репозиторий

- Сделано: проверены Python 3.14 и Git 2.55; установлен PostgreSQL 18 через winget, созданы пользователь и база `bugtracker` (UTF-8); создан Git-репозиторий, виртуальное окружение venv, `.gitignore`, `.env.example`; SQL-схема БД сохранена в `schema.sql` (добавлен статус rejected).
- Сложности: установщик PostgreSQL не добавляет `psql` в PATH, добавляли вручную; из-за разных кодировок консоли (866) и psql (1251) русское имя роли сохранилось «кракозябрами», решение — `chcp 1251` перед запуском psql.
- Инструменты: PowerShell, winget, psql, git (`status`, `check-ignore`, `log --stat`), парсер SQL pglast для проверки синтаксиса `schema.sql`.

## Этап 1. Каркас Flask-приложения

- Сделано: установлены Flask и python-dotenv (`requirements.txt`); настройки вынесены в `config.py` и читаются из `.env`; фабрика `create_app()` подключает 4 модуля (blueprints) — projects (`/`), auth (`/auth`), bugs (`/bugs`), admin (`/admin`); базовый шаблон `base.html` с меню, страницы-заглушки наследуют его, простой CSS.
- Сложности: циклический импорт между `__init__.py` и `routes.py` blueprint'а — решён импортом маршрутов в конце файла; русский текст в выводе консоли Windows отображается «кракозябрами» (кодировка консоли), на работу приложения не влияет.
- Инструменты: `flask run` в режиме отладки (автоперезапуск, отладчик в браузере, лог запросов в консоли), `flask routes` для проверки карты адресов, тестовый клиент Flask (`app.test_client()`) для проверки ответов страниц.

## Этап 2. Модели и миграции

- Сделано: подключены Flask-SQLAlchemy, Flask-Migrate и драйвер psycopg; в `app/models.py` описаны 7 таблиц (модели Role, User, Project, Bug, Comment, StatusHistory и связующая таблица project_members) с CHECK-ограничениями, внешними ключами, каскадным удалением и индексами; первая миграция создаёт все таблицы, вторая (миграция с данными) — роли admin/tester/developer.
- Решения по схеме: пересмотрели NULL — NOT NULL везде, где пустое значение бессмысленно (severity, priority с DEFAULT 'medium', updated_by, created_by, old_status, changed_by); nullable оставлены только описание, шаги, ожидаемый/фактический результат, окружение и исполнитель.
- Сложности: при откате и повторном применении миграции id ролей стали 4–6 — счётчик SERIAL не сбрасывается, поэтому роли в коде ищем по имени, а не по id.
- Инструменты: `flask db migrate/upgrade/downgrade/history/current`, ручная проверка сгенерированной миграции перед применением, проверка ограничений тестовыми INSERT в откатываемой транзакции, `flask shell`, psql (`\dt`, `\d bugs`).

## Этап 3. Авторизация и роли

- Сделано: пароли хранятся только как хэш (scrypt с солью, werkzeug); вход и выход через Flask-Login; формы входа и регистрации на Flask-WTF с русскими сообщениями и проверкой уникальности логина и почты; регистрация создаёт заблокированный аккаунт, который одобряет админ; заблокированный не может войти, а уже вошедшего выкидывает при следующем запросе; декоратор `role_required` (403 для чужой роли), админка только для admin; команда `flask create-admin`.
- Безопасность: CSRFProtect для всего приложения, выход только через POST с CSRF-токеном; сообщение о блокировке только после верного пароля; при ошибке — общее «Неверный логин или пароль»; защита от open redirect в параметре `next` (проверка из туториала через `urlsplit` пропускала `/\evil.com`, заменена своей).
- Сложности: проверка входа сначала «падала» — вошедшего перекидывало на вход. Причина была в самой проверке: запросы шли внутри одного общего контекста приложения, и Flask-Login брал «гостя» из кэша `g` предыдущего запроса. Нашли, распечатав содержимое сессии; в тестах каждый запрос должен идти в своём контексте.
- Инструменты: тестовый клиент Flask с настоящими POST-запросами и CSRF-токеном, `session_transaction()` для просмотра сессии, `test_cli_runner()` для проверки консольной команды, `flask routes` для проверки методов маршрутов.

## Этап 4. Проекты и участники

- Сделано: список проектов с учётом роли (админ видит все, остальные — только свои); создание и редактирование проекта (только admin, уникальность названия с учётом редактируемого проекта); страница проекта с участниками; добавление и удаление участников (только admin, POST с CSRF); общая проверка доступа `get_project_or_403` (нет проекта — 404, не участник — 403) для будущих страниц багов. По ходу этапа ТЗ дополнено редактированием проекта; удаление проекта сознательно не делаем — вместе с ним удалились бы баги.
- Безопасность: список пользователей для добавления формируется на сервере, SelectField отклоняет подменённый id (заблокированный, админ, уже участник); вывод через Jinja экранируется — `<script>` в описании показывается текстом (защита от XSS).
- Сложности: в проверках дважды получили 400 вместо ожидаемого 403 — CSRF-токен брался из другой сессии тестового клиента, и срабатывала CSRF-защита раньше проверки роли; с токеном той же сессии получили правильный 403. Вывод для тестов: токен брать тем же клиентом, что отправляет запрос.
- Инструменты: тестовый клиент Flask (проверки доступа для каждой роли, подмена данных формы), регулярные выражения для разбора HTML в проверках, `flask routes` (изменяющие маршруты — только POST).

## Этап 5. Баги: CRUD

- Сделано: создание бага (tester — участник проекта; серьёзность обязательна, приоритет по умолчанию medium); карточка бага с доступом как к проекту; списки багов на странице проекта и в разделе «Баги» (админ видит все); редактирование (только автор и admin, статус формой не меняется — подменённый `status` в запросе игнорируется); назначение исполнителя: автор и admin выбирают активного developer-участника, developer берёт свободный баг себе и может отказаться.
- Правила уточнены вместе с ТЗ и записаны в ТЗ: баг в работе без исполнителя возвращается в new (от имени того, кто снял назначение); при удалении участника из проекта с него снимаются назначения в этом проекте. Общие правила назначения вынесены в `app/assignments.py`, чтобы использовать их и при блокировке пользователя (этап 8).
- Сложности: макрос таблицы багов падал с `UndefinedError: 'STATUS_LABELS' is undefined` — Jinja импортирует макросы без контекста шаблона; исправлено `import ... with context`. В одной проверке объект проекта удалялся сборщиком мусора до записи изменения (ошибка в скрипте проверки, не в приложении).
- Инструменты: тестовый клиент Flask с проверкой каждой роли и подменой данных форм (чужой id, лишнее поле status), трассировка ошибки Jinja в логе Flask, повторный прогон прежних проверок после каждого изменения (регрессия).

## Этап 6. Статусы, триггер и история

- Сделано: триггер PostgreSQL `trg_bugs_status_history` (функция на PL/pgSQL, создаётся миграцией с сырым SQL) пишет в `status_history` каждую реальную смену статуса, автор — `bugs.updated_by`; правила жизненного цикла вынесены в `app/workflow.py` (таблица переходов «из → в → роль», дополнительные условия, понятные тексты ошибок); кнопки переходов в карточке показывают только доступные пользователю действия, отклонение — только с причиной (статус, комментарий и запись истории — одна транзакция); история статусов в карточке.
- Правила уточнены и записаны в ТЗ: кто именно выполняет каждый переход; взявший свободный баг становится исполнителем; `rejected → new` снимает исполнителя; `fixed → in_progress` без исполнителя ведёт в new; у closed/rejected исполнитель не меняется (память о том, кто исправил), в том числе при удалении из проекта и блокировке; заблокированный исполнитель помечается.
- Проверки: триггер — прямыми SQL-запросами и через приложение (UPDATE с тем же статусом и ROLLBACK историю не пишут, откат/повтор миграции, `flask db check`); правила — на объектах в памяти для каждой роли и статуса; весь цикл — HTTP-запросами. Проверка показала две проблемы интерфейса (две похожие кнопки «взять», «Отказаться» на закрытом баге) — исправлены.
- Инструменты: psql (`\df`, `\d bugs`, BEGIN/ROLLBACK для безопасной проверки), `flask db upgrade/downgrade/check`, тестовый клиент Flask, повторный прогон всех прежних проверок.

## Этап 7. Комментарии

- Сделано: список комментариев в карточке бага (от старых к новым, пометка «заблокирован» у неактивного автора) и добавление комментария (любой, кто видит баг — участники и admin, в любом статусе, включая закрытые). Редактирования и удаления нет сознательно: комментарии — журнал, как история статусов, в том числе причины отклонения.
- Решения: обязательный текст до 5000 символов, пробелы по краям обрезаются; статус бага от комментария не меняется; после отправки возврат к разделу комментариев по якорю `#comments`. Отдельное правило доступа не понадобилось — используется `get_bug_or_403`.
- Инструменты: тестовый клиент Flask (все роли, закрытый баг, пустой/пробельный/слишком длинный текст, запрос без CSRF), `flask routes` — у комментариев только POST-маршрут добавления.

## Этап 8. Админ-страница

- Сделано: список пользователей с тремя статусами («ожидает одобрения», «активен», «заблокирован»; заявки сверху); одобрение заявки с выбором роли; блокировка и разблокировка; смена роли; создание пользователя админом (сразу одобрен). Все действия — POST с CSRF, только для admin; себя заблокировать и сменить себе роль нельзя.
- БД (ПМ11): две отдельные миграции — колонка `users.approved_at` с переносом данных (уже активным approved_at = created_at) и CHECK `ck_users_active_approved` («одобрен ИЛИ неактивен»); миграция сначала чинит нарушителей, иначе PostgreSQL не создал бы ограничение. В psql проверено: запись `approved_at = NULL` при `is_active = true` отклоняется.
- Правила: блокировка и уход с роли developer снимают назначения с открытых багов во всех проектах (общая функция `unassign_user_bugs`, баг в работе → new с записью в истории от админа; closed/rejected не трогаются); разблокировка назначения не возвращает. Форма создания пользователя наследует форму регистрации — проверки не дублируются.
- Сложности: после CHECK часть старых проверок начала падать — они создавали активных пользователей без approved_at, а проверка регистрации «одобряла» заявку одним `is_active = true`; это ограничение и должно было запретить, проверки обновлены. Инструменты: psql в транзакции с ROLLBACK, `flask db upgrade/downgrade/check`, тестовый клиент Flask.

## Этап 9. Фильтры, статистика, VIEW

- Сделано: фильтры списка багов по статусу, серьёзности и исполнителю (GET-параметры в адресе, неизвестные значения игнорируются, фильтр только сужает видимые баги); два представления PostgreSQL через миграцию с сырым SQL — `v_bug_stats` (баги по проектам и статусам, LEFT JOIN для проектов без багов) и `v_open_bugs_by_assignee` (открытые баги по исполнителям в каждом проекте, включая «не назначен»); страница статистики читает представления, каждый видит только свои проекты, admin — все.
- БД (ПМ11): агрегаты `count(*) FILTER (WHERE ...)` — несколько счётчиков за один проход; разница `count(b.id)` и `count(*)` при LEFT JOIN; представления не хранят данные — после изменения бага следующий SELECT сразу показывает новые цифры; Alembic представления не трогает (`flask db check` чистый).
- Проверки: числа из представлений сверены с прямым подсчётом по таблице bugs; попытка отфильтровать по исполнителю из чужого проекта и «мусор» в адресе (`' OR 1=1`) ничего лишнего не показывают — значения идут в базу параметрами и дополнительно проверяются по словарям.
- Инструменты: psql (`\dv`, `\d+`), `flask db upgrade/downgrade/check`, тестовый клиент Flask.

## Этап 10. SQL-запросы, роли БД, резервное копирование

- Сделано: `sql/queries.sql` — показательные запросы (JOIN, агрегаты с HAVING и AVG по интервалам, EXISTS/NOT EXISTS, история статусов, представления, INSERT/UPDATE/DELETE с RETURNING, SAVEPOINT) — изменения в BEGIN … ROLLBACK; `sql/roles.sql` — роли `bugtracker_app` (только нужные операции: журналы comments/status_history — SELECT/INSERT, DELETE только в project_members, без CREATE/ALTER/DROP) и `bugtracker_readonly` (SELECT без `password_hash` — права на колонки); `docs/backup.md` — pg_dump/pg_restore.
- Проверки сохранены в репозиторий: `sql/check_roles.sql` (33 пробы с ожидаемым результатом, откатывается), `tests/test_db_roles.py` (те же пробы на pytest; «запрещено» засчитывается только при ошибке 42501), `docs/test_scenarios.md` (24 HTTP-сценария, пройденных приложением под `bugtracker_app` без единой ошибки 5xx).
- Резервная копия проверена восстановлением в отдельную базу: содержимое всех таблиц совпало по md5, триггер в восстановленной базе работает. Роли в копию не входят — это объекты сервера.
- Сложности: пароли для ролей не должны попасть ни в файлы, ни в историю команд — `\prompt` в psql; ошибки PostgreSQL выводятся по-русски в кодировке консоли — для автоматической проверки сверяли код SQLSTATE (42501), а не текст. Инструменты: psql (`\gset`, `\if`, `VERBOSITY verbose`), pg_dump, pg_restore, pytest.

## Этап 11. Тесты pytest и Playwright

- Сделано: 144 теста. Модульные тесты правил жизненного цикла на объектах в памяти (47); тесты с отдельной базой `bugtracker_test` — схема пересоздаётся миграциями, права ролей выдаёт тот же `sql/grants.sql`, данные очищаются перед каждым тестом; права доступа с автоматическим обходом всех маршрутов `app.url_map` (вход обязателен, действия только POST, CSRF, админка); вход/выход/регистрация/create-admin; 24 сквозных сценария под ролью `bugtracker_app`; права ролей БД (35); три сквозных теста в браузере на Playwright (вход, создание бага, смена статуса) с «живым» сервером в отдельном потоке.
- Качество тестов проверяли внесением ошибок в код (снятый `@login_required`, GET у действия, выключенный CSRF, лишнее право в таблице переходов, пропавшее право INSERT у роли, форма без CSRF-токена): тесты должны падать. Так нашли две дыры в самих тестах — действие, принимающее GET, не проверялось; второй уровень защиты в `user_loader` не был покрыт — дописали тесты.
- Сложности: очистка базы после `yield` в фикстуре стирала данные и упавшего теста — перенесли очистку до теста; CSRF-токен нужно брать той же сессией; после `git checkout` файлы получали окончания строк Windows и шаблоны поломок не совпадали; Chromium для Playwright не скачался из среды разработки — тесты проверены в установленном Microsoft Edge (`--browser-channel msedge`).
- Инструменты: pytest (фикстуры, parametrize, маркеры, `pytest.raises`, `-m "not e2e"`), тестовый клиент Flask, `test_cli_runner`, Playwright (`--headed --slowmo`), psycopg для выполнения SQL-файлов.

## Этап 12. Доработка по ТЗ, ruff, README

- Сделано: по сверке с разделом «Функционал» закрыты два пропущенных пункта — страницы ошибок 400/403/404/500 (в обработчике 500 откат «сломанной» транзакции) и журнал в `logs/bugtracker.log` с ротацией (входы, неудачные попытки, отказы 403, действия администратора, ошибки 500; пароли не пишутся); демо-данные `flask seed` (аккаунты pixel/ana/bob, 5 багов во всех статусах, история от триггера, без дубликатов) и `flask seed --restore-demo` (только демо-аккаунты; демо-аккаунт = логин + почта @demo.example — защита от захвата настоящего аккаунта); линтер ruff (PEP 8, ошибки, порядок импортов, flake8-bugbear) — код без замечаний; README целиком.
- Сложности: строка «запущен» в журнале писалась при любой команде flask, а в режиме отладки — дважды (flask run создаёт процесс-наблюдатель и процесс-сервер); теперь пишется только процессом-сервером. Без flush после каждого перехода демо-данные падали: связь assignee уже установлена, а колонка assignee_id заполняется только при отправке в базу. Два утверждения в README (про права для flask seed и версию Python) оказались непроверенными — проверили и исправили.
- Инструменты: ruff (`check`, `--diff`, `--fix`), pytest (caplog, tmp_path, monkeypatch, test_cli_runner), запуск реального сервера и подсчёт строк журнала, внесение ошибок в код для проверки тестов.

## Этап 13. Деплой на Render, Qlty

- Сделано: приложение и PostgreSQL 18 на Render (бесплатный тариф, Frankfurt) — https://bugtracker-9p9c.onrender.com/; сервер gunicorn, запуск `flask seed && gunicorn`, сборка из `requirements.txt` (только runtime), версия Python из `.python-version`; настройки только в переменных окружения Render (свой случайный SECRET_KEY, журнал в консоль `LOG_TO_STDOUT=1`, cookie входа только по HTTPS `SECURE_COOKIES=1`, отладка выключена); README — раздел «Деплой», бейдж Qlty, демонстрация.
- БД (ПМ11): сайт работает под ролью `bugtracker_app` с минимальными правами, миграции и права (`grants.sql`) выполняет владелец со своего компьютера, его адрес на Render не хранится; `roles.sql` пришлось доработать — на хостинге у владельца есть CREATEROLE, но нет SUPERUSER (убран `NOSUPERUSER`), и пароли по умолчанию хэшировались в устаревшем MD5 (включён `scram-sha-256`). Права ролей на Render проверены `check_roles.sql` (33 из 33). Бесплатная база живёт 30 дней — порядок переноса через pg_dump/pg_restore описан в README.
- Сложности: подключение новых ролей снаружи через прокси Render отклонялось проверкой SCRAM channel binding — `channel_binding=disable` при `sslmode=require`; проверка сайта скриптом получала 400 от CSRF — по HTTPS Flask-WTF требует заголовок Referer (браузер его шлёт); cookie сессии приходила без флага Secure — добавлена настройка и тест; адрес базы в PowerShell вводим через `Read-Host`, чтобы пароль не попал в историю команд.
- Инструменты: панель и логи Render, psql и psycopg по внешнему адресу базы, pg_dump/pg_restore, curl и Python-скрипт для проверки сайта снаружи (коды ответов, вход демо-аккаунтами, флаги cookie, сертификат TLS), pytest, ruff, Qlty.
